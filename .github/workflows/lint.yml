name: Lint

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint:
    name: Lint (Python 3.14)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install dependencies
        run: |
          uv sync --all-groups

      # --no-fix обязателен: pyproject задаёт ruff с `fix = true`,
      # и без флага CI починил бы найденное и позеленел.
      - name: Run ruff
        run: |
          uv run ruff check --no-fix .

      - name: Run mypy
        run: |
          uv run mypy --config-file pyproject.toml

      - name: Run codespell
        run: |
          uv run codespell src examples

      - name: Run slotscheck
        run: |
          PYTHONPATH=src uv run slotscheck -m maxo

      - name: Run bandit
        run: |
          uv run bandit -c pyproject.toml src -r
